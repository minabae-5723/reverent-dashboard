# ============================================================
#  Secrets — NEVER commit
# ============================================================
config.json
config.json.json
*.key
*.pem
.env
.env.*

# ============================================================
#  Runtime cache (regenerated on every refresh)
# ============================================================
data.json
calendar.json
calendar-week.json

# ============================================================
#  Server logs
# ============================================================
server.log
server.err
*.log
*.err

# ============================================================
#  Binaries (cloudflared, etc.)
# ============================================================
*.exe
*.dll
*.zip

# ============================================================
#  OS / IDE
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~

# ============================================================
#  OneDrive sync conflict files
# ============================================================
*-conflicted*
*-charging*

# ============================================================
#  Python / Node (if added later)
# ============================================================
__pycache__/
*.pyc
node_modules/

# ============================================================
#  Keep an empty news/ folder structure (gitkeep), but include
#  all .md files (users want to share clippings)
# ============================================================
# news/ → committed

# ============================================================
#  Deal Angle Radar — raw scan + extracted filing text are
#  local working files; only YYYY-MM-DD.md + index.json deploy
# ============================================================
deal-angle/raw-*.json
deal-angle/docs/
